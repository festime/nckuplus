.row
  %h1 編輯貼文
  - if @post.errors.any?
    %ul
      = @post.errors.full_messages.each do |message|
        %li= message
  = form_for @post do |f|
    .form-group
      = f.label :content, "內文"
      = f.cktext_area :content, rows: 10
    .form-group
      = f.submit "發文", class: "btn btn-primary"
